# Data

Place the raw dataset here:

```text
projects/retail-customer-segmentation/data/raw/clientes_retail_segmentacion.csv
```

The analysis script can also download the dataset from the public course URL when the local CSV is missing.

Raw data is intentionally not committed to GitHub. The repository publishes the reproducible code, generated figures, and summary outputs while keeping raw data local unless redistribution rights are explicit.
