# Data

Raw data is not included in this repository.

Place the local dataset here:

```text
projects/employee-attrition-prediction/data/raw/WA_Fn-UseC_-HR-Employee-Attrition.csv
```

The `data/raw/` folder is ignored by Git, so the dataset can be used locally without being published.
